import 'package:flutter/material.dart';
import 'package:internet_checker_app/app/app.dart';

void main() async {

  // Initialization Flutter bindings
  WidgetsFlutterBinding.ensureInitialized();

  runApp(const MyApp());
}