name: internet_checker_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # Icons Packages
  iconsax: ^0.0.8
  cupertino_icons: ^1.0.8

  # Internet Connectivity Packages
  connectivity_plus: ^6.1.3

  # State Management Packages
  flutter_bloc: ^9.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true